/** \file compare.h
 *  \brief Compare floats with an accuracy.
 */
#ifndef COMPARE_H
#define COMPARE
/** \param[in] x First number.
 *  \param[in] y Second number.
 *  \return 0 if x = y, <0 if x < y, >0 if x > y.
 */
double CompareFloat(double x, double y);
#endif
